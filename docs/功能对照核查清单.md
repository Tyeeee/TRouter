# TRouter 功能对照核查清单（对开发文档逐条复核）

> 目的：回答「demo 看不出所有功能都开发了 / 是否有遗漏」。对照文档 = 各版本方案与《版本回顾与缺口核查》。
> 核对时间：2026-09-05（L4 完成节点）。方法：逐特性 grep 代码 + demo 入口 + 测试证据，非口头保证。

---

## A. 原始路线图承诺（V1.0–V5.0）——每项给出代码证据与 demo 入口

| 里程碑特性 | 代码证据 | Demo 入口 | 验证测试 | 状态 |
|---|---|---|---|---|
| @Route 注解 + KSP 分组生成 | `trouter-annotation/Route.kt`、`TRouterProcessor.kt`、每模块 `generated/GroupLoader_*` | （构建期） | C1/C2/C3 编译门 | ✅ |
| 同模块 path 冲突=编译 ERROR / 字面量 Warning | `TRouterProcessor` | （构建期） | `compile-gate-duplicate.log` 等 | ✅ |
| 惰性加载（init/install 不加载页面类） | 生成物只含字符串；`Class.forName` 仅在 `openTarget` | 启动日志无页面类加载 | O1 GroupLoader 日志 | ✅ |
| init/install/navigate 统一入口 | `TRouter.kt` | 主页全部行 | MainRouterTest | ✅ |
| 未注册路径 NotFound + onLost 降级 | `navigateInternal` | **S04 行**（点击→状态「未找到路由」） | testLostNavigation | ✅ |
| isDebug 日志开关 | `TRouter.log()` | 主页 D 区说明（无独立页） | testConfigDebugMode | ✅ |
| FRAGMENT 目标承载 | `FragmentContainerActivity` + DemoFragment | **S02 行**（点击→Fragment 演示页） | testFragmentNavigation | ✅ |
| 分组路由（default/secondary/mock） | 生成 GroupLoader 三组 | 快照行 group=… | testSecondaryGroupNavigation | ✅ |
| 同任务返回语义（返回键回主页） | 生命周期绑定 + singleTask | 各目标页「返回」 | 返回语义测试/截图 | ✅ |
| 页面自述 + 运行时元数据（RouteLaunch） | 各目标页横幅 | 每个目标页顶部 | — | ✅ |
| 路由表快照（registeredRoutes） | `TRouter.registeredRoutes` | 主页快照区（带 ↦ 模块来源） | — | ✅ |
| 拦截器链 Continue/Block/Redirect | `RouteInterceptor`/`InterceptorDecision` | E 区 S08（门禁）/S09（Mock 开关） | Interceptor 契约+UI | ✅ |
| Blocked 结果（密封类无 null） | `TRouterResult.Blocked` | S08 开→S01 状态「已拦截」 | 契约用例 | ✅ |
| 日志 4→6→8 节点 | [navigate]/[interceptor]/[remote] | logcat | 日志断言 | ✅ |
| 多模块：feature 各自产路由 + host 聚合 | feature-demo/about + `DemoRouteRegistry` | 快照行 ↦ 模块标签 | 聚合唯一性契约 | ✅ |
| 跨进程路由（AIDL + @CrossProcess + 白名单） | `IRouterService`/`RemoteRouterService`/`navigateRemote`/CrossProcessPaths | **S11 行**（点击→第二进程页面 pid=…） | Remote 契约+UI | ✅ |
| 动态路由 register/unregister | `TRouter.registerRoute/unregisterRoute` | **S13 行**（注册/注销切换）+ S14 导航 | TRouterDynamicContractTest | ✅ |
| 路由图谱 routeGraph | `RouteGraph.kt` + `routeGraph()` | 主页「图谱摘要：节点 N · 边 M」 | 图谱一致性契约 | ✅ |

## B. 各里程碑"无 UI 属设计"的场景（demo 上看不到正常，证据在测试/日志）

| 场景 | 为何无 UI | 证据 |
|---|---|---|
| S05 isDebug 开关 | 测试注入配置驱动（演示页无法切换进程内 config） | testConfigDebugMode |
| S06 主页自身 | 主页即 /main | 启动即覆盖 |
| S07 API 契约（NotInitialized/幂等/traceId/NotFound） | 契约类 | TRouterApiContractTest |
| S10 拦截器契约 | 契约类 | TRouterInterceptorContractTest |
| S12 跨进程契约（白名单/未配置/远端 NotFound） | 契约类（真实 AIDL 双进程） | TRouterRemoteContractTest |
| S15 洋葱链契约（L4） | 契约类 | TRouterOnionChainTest |

## C. 补全批（你已批准，分批执行中）——**以下为真·未完成，非遗漏**

| 条目 | 状态 | 说明 |
|---|---|---|
| L4 洋葱/双向链（WrappingInterceptor/ChainOutcome） | ✅ 已实施（本批第 1 项） | 6 例洋葱 + 旧拦截器 12 例全绿 |
| L2 动态增删拦截器（add/removeInterceptor） | ⏳ **未实施**（计划第 2 项） | grep 确认 API 不存在 |
| L3 @Interceptor 注解级拦截 | ⏳ 未实施（第 3 项） | 方案 §3 已确认默认 |
| F 动态路由热更/持久化 | ⏳ 未实施（第 4 项） | 方案 §4 已确认默认 |
| L1 跨模块编译期冲突检测 | ⏳ 未实施（第 5 项） | 方案 §5（构建期校验任务） |
| D 动态代理（V4 增强） | ⏳ 未实施（第 6 项） | 方案 §6 = 语义① AIDL 透明代理封装 |

> 因此 demo 目前看不到 L2/L3/F/L1/D 是**符合分批进度**的：你批准后我只完成了 L4，其余 5 项待继续（每项完成即配专属用例）。

## D. 建议：让 demo"看得出全部"（可选 UI 增强）

1. 主页顶部加「功能一览（只读）」区：V1–V5 + 补全批各一条：特性名 + 对应场景号/入口 + 状态（✅/契约-only/进行中）。
2. 现有场景行已覆盖 S01–S04、S08、S09、S11、S13、S14（可点）；S02/S03 等需滚动。无独立行的均为契约场景——功能一览可解释清楚，避免观感"没做全"。
