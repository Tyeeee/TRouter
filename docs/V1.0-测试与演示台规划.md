# TRouter V1.0 测试台系统化规划（评审稿 v0.1 · 已按确认实施）

> 状态：**已实施**（2026-09-04）。主页场景索引（S01–S06 展示 + S05 说明 + 路由表快照）、目标页自述横幅、用例对齐场景号均已落地；
> 全量 10 用例在 pixel_6 模拟器连跑两遍全绿；导览见 `docs/demo/README.md`，验收证据见 `docs/reports/v1/V1.0-TEST-REPORT.md`。
> 开放项结论：O1=core 只读快照（`TRouter.registeredRoutes()`）自动列出；O2 采用 S01–S07 七场景；O3 中文文案；O4 含首页 UI 重构。
> 目标：让**任何陌生开发者**拿到本 demo 后，仅凭界面与测试代码就能看懂「每个页面/每个用例在验证什么、为什么这样验证、对应哪条验收」。
> 现状问题（本规划要消除的）：主页 = 一排无上下文的按钮；用例方法与场景无显式编号关联；页面不说明自己是谁、从哪条路由来。
> 配套文档：V1.0-工程搭建方案 / V1.0-测试方案 / V1.0-TEST-REPORT.md。

---

## 1. 设计原则（可读性验收标准）

| # | 原则 | 验收口径（陌生人能否……） |
|---|---|---|
| P1 | 场景编号制 | 界面上每个可执行场景有稳定编号（S01…），代码/报告/文档三处同号可互查 |
| P2 | 页面自述 | 每个目标页顶部横幅说明：场景号、页面名、路由 path、group、目标类型（Activity/Fragment）、期望结果 |
| P3 | 索引分级 | 主页按「验证类别」分组罗列场景（如 导航成功 / 多态目标 / 分组路由 / 降级 / 配置可观测），而非平铺按钮 |
| P4 | 一键看懂 | 每行场景含「一句话做什么 + 路径 + 期望」，不需读代码即知该点开验证什么 |
| P5 | 用例对齐 | 每个 Instrumentation 用例标注它驱动的场景号；场景清单页与用例一一映射，无孤儿按钮、无孤儿用例 |
| P6 | 路由目录可查 | 主页附带「当前已注册路由表」只读清单（path/group/kind/目标类），证明注册正确且可观测（实现方式见开放项 O1） |

---

## 2. 场景体系（V1.0 验收域 → 场景）

### 2.1 场景总表

| 场景号 | 验证类别 | 场景名 | 入口操作 | 目标 path / group / kind | 期望结果 | 对应验收 | 对应用例（方法） |
|---|---|---|---|---|---|---|---|
| S01 | 导航成功 · Activity | 基础页面跳转 | 索引点开 S01 | `/second` · default · ACTIVITY | Second 页展示；返回 Success | R1/O1 | testBasicNavigation |
| S02 | 导航成功 · Fragment | Fragment 目标承载 | 索引点开 S02 | `/fragment-demo` · default · FRAGMENT | Fragment 演示页在 core 容器中展示；kind=FRAGMENT | R1(FRAGMENT)/O1（A3 补全） | testFragmentNavigation |
| S03 | 分组路由 | secondary 分组加载 | 索引点开 S03 | `/about` · secondary · ACTIVITY | About 页展示；日志出现 group=secondary 加载起止 | C3/R1/O1 | testSecondaryGroupNavigation |
| S04 | 降级处理 | 未注册路径 | 索引点开 S04 | `/not/exist`（刻意不注册） | 不崩溃；状态栏「未找到路由:/not/exist」；onLost 回调；NotFound 返回 | R2/O1 | testLostNavigation / lostPathReturnsNotFoundAndTriggersOnLost(API) |
| S05 | 配置 · 可观测 | isDebug 日志开关 | 主页说明（用例驱动） | —（由测试注入配置） | debug=true 四节点日志输出；debug=false 静默但导航正常 | R3/T1 | testConfigDebugMode |
| S06 | 导航成功 · 主页自身 | 主页也是注册路由 | 索引点开 S06（或直接启动） | `/main` · default · ACTIVITY | Main 页展示 | R1 | （启动即覆盖，附断言可选） |
| S07 | API 契约（无 UI） | 未初始化 / 幂等 / traceId | 无（测试直接调用） | — | NotInitialized、重复 install 幂等、traceId 贯穿 | 契约分支 | TRouterApiContractTest.* |

> 映射规则：**每个场景号唯一**；UI 可驱动场景在 MainRouterTest 有对应 @Test；非 UI 契约归 TRouterApiContractTest；示例 ExampleInstrumentedTest 不计入场景体系。

### 2.2 场景内文本规范（写进每个页面横幅，统一模板）

```
场景 S02 · Fragment 目标承载
路径   /fragment-demo   分组 default   类型 Fragment
期望   Fragment 演示页经 core 容器展示；navigate 返回 Success(kind=FRAGMENT)
```

---

## 3. 页面信息架构（改造后）

```
TRouterDemo（App 名 + 副标题：TRouter V1.0 测试台）
├── 场景索引页（MainActivity，@Route /main）
│   ├── 头部：说明一行 + 「路由表快照（只读）」（可选，见 O1）
│   └── 场景分组（每组标题 + 若干行，行=可点击 + 说明文字）：
│       ├── [A] 导航成功——Activity / Fragment / 主页本身 → S01 / S02 / S06
│       ├── [B] 分组路由 → S03
│       ├── [C] 降级处理（未注册路径）→ S04
│       └── [D] 配置与可观测性（isDebug 开关）→ S05（页内说明，由用例驱动）
├── SecondActivity（S01）横幅自述 + 返回
├── FragmentContainerActivity(core) + DemoFragment（S02）横幅自述
├── AboutActivity（S03）横幅自述 + 返回
└── （S05 无独立页：由 MainRouterTest 两阶段驱动并在报告中展示日志）
```

每行场景的视觉模型（取代“光秃秃按钮”）：

```
┌──────────────────────────────────────────────┐
│ S01  基础页面跳转（导航成功 · Activity）          │
│      路径 /second · group default · → 点开       │
│      期望：Second 页展示，navigate 返回 Success   │
└──────────────────────────────────────────────┘
```

实现建议：纯 View 代码构造行（title/subtitle/id=`scenario_s01`…），不引入 RecyclerView/Compose，保持 Espresso 按 id 点击稳定。

---

## 4. 测试代码可读性改造

| 文件 | 改动 |
|---|---|
| `app/src/androidTest/.../MainRouterTest.kt` | 每个 @Test 上方 KDoc 写「驱动场景 S0x + 期望」，断言顺序=索引行描述顺序；类头注释说明「本类=UI 行为驱动场景」 |
| `.../TRouterApiContractTest.kt` | KDoc 标注覆盖场景 S07 各子分支；加「为何不能 UI 驱动」一句话 |
| `app/src/main/.../MainActivity.kt` | 改为场景索引（分组渲染 + 最近一次结果状态栏）；保留 S04 状态回显 |
| 目标页（Second/DemoFragment/About） | 顶部横幅自述（P2） |
| `docs/` | 新增《测试台导览 README》供陌生人从 repo 首页进入；V1.0-TEST-REPORT.md 增补场景号列 |

断言策略不变：**行为驱动为主**（S01–S05 用 Espresso 点击索引行），仅 S07 契约分支直调 API。

---

## 5. 落地改动清单（待确认后执行）

1. `MainActivity`：重构为场景索引（P3/P4），行 id `scenario_s01..s07(无UI的除外)`；保留 status 回显。
2. 目标页横幅（SecondActivity / DemoFragment / AboutActivity / MainActivity 自身说明）。
3. 新增/更新 strings（或直接代码文案常量，保持单一来源 `ScenarioText` 对象便于测试断言复用）。
4. 测试类注释与断言微调（不改变既有用例名与 DoD 语义），新增场景号映射注释。
5. 若 O1=自动目录：core 增加只读 `TRouter.registeredRoutes(): List<RouteMeta>`（拷贝快照）并在索引页展示。
6. 复跑两遍全绿 + 更新截图/报告（含场景号列）。
7. 新增 `docs/demo/README.md`（导览：怎么装、怎么跑、场景→验收对照表）。

## 6. 待你确认的开放项

- **O1 路由表快照**：主页「已注册路由表」用 ①core 只读 API 自动列出（推荐，直观证明注册/分组/惰性）还是 ②人工在索引页维护列表（不扩 core API）？
- **O2 场景粒度**：S01–S07 七场景是否足够，或需再加（如：返回/生命周期、重复 install 演示页）？
- **O3 文案语言**：页面/用例注释用中文（当前）还是中英双语？
- **O4 范围确认**：本次是否包含「把首页改造成索引」这一可见 UI 重构（涉及既有截图与报告更新）？
