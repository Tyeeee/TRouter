# TRouter V5.0 动态路由 + 路由图谱 · 测试报告

> 状态：**完成待统一回测**。V5.0 方案（`docs/V5.0-动态路由与图谱方案.md`）O1–O5 采纳推荐默认实施；
> 按你确认的测试协议：**本版只测 V5 新功能（5 例 ×2 遍全绿），未做 V1–V4 历史回测**——统一回测于功能里程碑全部完成后集中执行。
> 功能里程碑至此收齐（V1 导航 / V2 拦截器 / V3 多模块 / V4 跨进程 / V5 动态路由+图谱）。

---

## 1. 环境与范围

| 项 | 值 |
|---|---|
| 设备 | AVD `trouter_test`（emulator-5554，API 36，pixel_6） |
| 构建 | AGP 9.3.2 / Gradle 9.5.0 / Kotlin 2.2.10 / KSP 2.2.10-2.0.2 / JDK 17 |
| 本次改动 | core：`registerRoute` / `unregisterRoute` / `routeGraph()` + `RouteGraph` 模型 + RouteTable.remove + 注册/注销/冲突日志；app：`DynamicDemoActivity`（**未标 @Route**）、RouterContract.PATH_DYNAMIC_DEMO、主页 G 区 S13/S14 行 + 图谱摘要 |
| 明确不含 | 动态路由持久化/热更、跨进程路由表同步、拦截器动态增删（L2）、@Interceptor（L3）、图谱可视化 SDK、统一回测（最后执行） |

## 2. 验收矩阵（V5.0 DoD，只测本版）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| C1 | 运行时注册/注销；注册后可导航、注销后回 NotFound | ✅ | S13-1；UI `testDynamicRouteScenario`；logcat `[route][register]`/`[route][unregister]` |
| C2 | 冲突拒绝 + 告警日志（不静默覆盖，first-wins） | ✅ | S13-2 `duplicateDynamicRegistrationIsRejected` |
| C3 | 同表共存：拦截器/NotFound/onLost/惰性/快照自动生效 | ✅ | S13-1（NotFound+onLost 分支）；S13-3（静态路由可注销语义统一） |
| C4 | 图谱 = 静态+动态同图、节点由边推导去重、随注册/注销增减 | ✅ | S14 `graphIncludesStaticAndDynamicAndStaysConsistent` |
| C5 | 注册/注销结构化日志；图谱进 registeredRoutes 体系 | ✅ | logcat 样本（§4）；主页图谱摘要「节点 N · 边 M」 |
| O1 | 图谱可被 demo/测试确定性读取 | ✅ | routeGraph() 纯模型 + 确定性插入序 |
| T1 | 本版只测 V5 用例 | ✅ | 5 例 ×2 全绿（run-v5-first/second） |

## 3. 用例清单（V5 本版 5 例）

| 场景 | 用例 | 断言要点 |
|---|---|---|
| S13 | `TRouterDynamicContractTest#registerNavigateThenUnregister` | 注册前 NotFound+onLost → 注册后 Success(目标类正确) → 注销后恢复 NotFound；注册/注销日志 |
| S13 | `...#duplicateDynamicRegistrationIsRejected` | 重复注册拒绝（不覆盖）、保留先注册者、冲突日志 |
| S13 | `...#unregisterStaticRouteWorks` | 静态路由可注销（路径级统一），注销后走降级 |
| S14 | `...#graphIncludesStaticAndDynamicAndStaysConsistent` | 边数=路由数、节点=类去重；注册后动态边/节点入图；注销后消失 |
| S13 | `MainRouterTest#testDynamicRouteScenario`（UI） | 真实点击：注册→导航 DynamicDemo 页；返回→注销→再导航 NotFound+onLost |

## 4. 手动走查与日志

| 步骤 | 结果 | 证据 |
|---|---|---|
| 主页 G 区 | S13/S14 行 + 图谱摘要行（节点=类、边=路由） | `21-v5-home-graph.png` |
| 点 S13 注册 → 点 S14 导航 | DynamicDemo 页打开（横幅「未标注 @Route」+ 运行时证据 /dynamic-demo） | `20-v5-dynamic-opened.png` |
| logcat | `[route][register] path=/dynamic-demo group=dynamic … DynamicDemoActivity` → navigate Success(/dynamic-demo) → `[route][unregister] path=/dynamic-demo` | `logs/logcat-v5-manual.txt`（全文见链接） |

## 5. 自动化执行记录（仅 V5）

| 轮次 | 文件 | 结果 |
|---|---|---|
| run-v5-first | `docs/reports/v5/instrumentation/run-v5-first.log` | 5/5 全绿 |
| run-v5-second | `docs/reports/v5/instrumentation/run-v5-second.log` | 5/5 全绿 |

## 6. 改动清单

| 位置 | 改动 |
|---|---|
| `trouter-core/.../api/RouteGraph.kt` | **新增** RouteGraph + RouteGraphEdge（只读模型） |
| `trouter-core/.../api/TRouter.kt` | registerRoute/unregisterRoute/routeGraph + `[route]` 日志；未初始化守卫 |
| `trouter-core/.../internal/RouteTable.kt` | 新增 `remove(path)` |
| `trouter-core/.../api/RouterContract.kt` | PATH_DYNAMIC_DEMO=/dynamic-demo + KDoc 演进 V5.0 |
| `app/.../DynamicDemoActivity.kt` | **新增**：无 @Route 的动态目标页（自述 + 运行时证据） |
| `app/.../MainActivity.kt` / ids / manifest | G 区 S13（注册/注销切换）+ S14（导航）+ 图谱摘要 refreshGraph |

## 7. 遗留与说明

1. 按测试协议：本报告**未**跑 V1–V4 历史 26 例（各版本当时已绿）；统一回测最后执行（预计 26 + 5 + 回测专项）。
2. 动态路由持久化 / 热更下发不在 V5 范围（roadmap 仅「动态路由与图谱可视化」，已实现）；如需热更再评估。
3. 动态注册不校验目标类存在（保持惰性与静态一致）：类不存在时 navigate 走既有降级（NotFound+onLost）。

## 8. 下一步门槛（进入统一回测）

- **功能里程碑已全部完成**（V1–V5）。请指示启动**统一回测**：全量 31 例 + 跨进程专项故障演练（服务断开重连/并发请求/失败路径）+ 手动全场景走查；通过后产出最终总报告并收尾框架。
